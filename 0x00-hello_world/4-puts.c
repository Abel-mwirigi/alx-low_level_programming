#include<stdio.h>
/**
 * main - Entry points a program thats prints a line with puts function
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
