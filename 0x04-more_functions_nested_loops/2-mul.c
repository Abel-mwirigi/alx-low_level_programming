#include "main.h"
#include <stdio.h>

/**
 * mul - product of two numbers
 * @a: the first variable
 * @b: second variable
 * Return: Always 0
 */

int mul(int a, int b)
{

	int result = a * b;

	return (result);
}
